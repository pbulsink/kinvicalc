% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data-models.R
\name{build_sample_result}
\alias{build_sample_result}
\title{Build a sample result record.}
\usage{
build_sample_result(
  viscometer_id,
  sample_type,
  analysis_temperature_c,
  time_1,
  time_2,
  min_flow_time_s = MIN_FLOW_TIME_S,
  operator = NA_character_,
  sample_id = NA_character_,
  notes = NA_character_
)
}
\arguments{
\item{viscometer_id}{Viscometer identifier.}

\item{sample_type}{Sample type label (see \code{.default_sample_rules()}).}

\item{analysis_temperature_c}{Analysis temperature in C.}

\item{time_1}{First measured flow time, s.}

\item{time_2}{Second measured flow time, s.}

\item{min_flow_time_s}{Minimum flow time, s, below which a result is
flagged. Defaults to 200 s (ASTM D445-26 6.1.2/10.2); override with the
viscometer/size-specific minimum from Specifications D446 when it is
higher.}

\item{operator}{Optional operator/user identifier performing the
determination. Recorded on the result for traceability.}

\item{sample_id}{Optional sample identifier/name being tested. Recorded on
the result for traceability.}

\item{notes}{Optional notes.}
}
\value{
A list-like result object. If the determinability check fails,
the result is still returned (with \code{determinability_result == "fail"})
so the caller/UI can prompt for a repeat measurement per ASTM D445-26
11.2.3/12.4.1 -- \code{kinvicalc} does not silently average a failing pair.
}
\description{
Assembles the two determined kinematic viscosity values (from the two
flow-time measurements on the same viscometer, ASTM D445-26 11.2/14.1),
checks determinability, and -- if the two values agree -- averages them
into a single reported result. This package does not apply a kinetic
energy correction; instead, any flow time below \code{min_flow_time_s} is
flagged (\code{low_flow_time_flag}) so it can be surfaced in reports for
operator review (ASTM D445-26 6.1.2/10.2).
}

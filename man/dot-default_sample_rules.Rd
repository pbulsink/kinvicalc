% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{.default_sample_rules}
\alias{.default_sample_rules}
\title{Default sample-type precision rules, transcribed from ASTM D445-26.}
\usage{
.default_sample_rules()
}
\value{
A tibble of seed precision rules with columns \code{sample_type},
\code{temp_min_c}, \code{temp_max_c}, \code{coefficient_a}, \code{exponent_b}, \code{offset},
\code{notes}, and \code{metric}.
}
\description{
Builds the seed data for the \code{sample_types} registry table (D445-26
Section 17, Precision and Bias):
\itemize{
\item determinability (d): 17.1.1 (table) and 17.1.2 (used in-service
oils); 11.2.4/12.4.1 fallback estimates for materials/temperatures not
listed.
\item repeatability (r): 17.2.1.
\item reproducibility (R): 17.2.2.
}
}
\details{
Each rule expresses its limit as a function of the average of the two
compared values, y or x (mm2/s):

\code{limit = coefficient_a * (average + offset) ^ exponent_b}

covering every functional form used in the standard:
\itemize{
\item \code{"0.0037y"} -> \code{coefficient_a = 0.0037}, \code{exponent_b = 1}, \code{offset = 0}
\item \code{"0.0013(y+1)"} -> \code{coefficient_a = 0.0013}, \code{exponent_b = 1}, \code{offset = 1}
\item \code{"0.00106y^1.1"} -> \code{coefficient_a = 0.00106}, \code{exponent_b = 1.1}, \code{offset = 0}
\item a fixed mm2/s value -> \code{coefficient_a = value}, \code{exponent_b = 0}, \code{offset = 0}
}

Some cells in the standard's repeatability/reproducibility tables for jet
fuels at -40 C, kerosine/diesel/biodiesel fuels and blends at 40 C, and
used in-service formulated oils could not be unambiguously transcribed
from the scanned/OCR'd table layout (columns for several sample types run
together). Those rows are intentionally left with \code{coefficient_a = NA} so
that \code{get_sample_type_rule()} errors loudly rather than silently using a
guessed value; confirm the correct figures directly against a clean copy
of D445-26 Table entries in 17.2.1/17.2.2 and update via
\code{add_sample_type_rule()}.
}
\keyword{internal}

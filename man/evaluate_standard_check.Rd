% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/app.R
\name{evaluate_standard_check}
\alias{evaluate_standard_check}
\title{Evaluate a measured result against a known standard/expected value.}
\usage{
evaluate_standard_check(
  sample_type,
  analysis_temperature_c,
  measured_value,
  expected_value,
  metric
)
}
\arguments{
\item{sample_type}{Sample type label passed to \code{get_sample_type_rule()}
(typically \code{"standard"}).}

\item{analysis_temperature_c}{Analysis temperature in C.}

\item{measured_value}{The measured kinematic viscosity, mm^2/s.}

\item{expected_value}{The known/expected kinematic viscosity for the
reference substance, mm^2/s. The precision limit is centred on this
value.}

\item{metric}{Either \code{"repeatability"} or \code{"reproducibility"}, passed to
\code{get_sample_type_rule()}.}
}
\value{
A list with \code{difference} (absolute difference between measured and
expected values), \code{limit} (the permitted precision limit), \code{result}
(\code{"pass"} or \code{"fail"}), and \code{passed} (logical).
}
\description{
Repeatability/reproducibility checks against a known Characterization
Laboratory QA/QC Standard Reference Substance value are centred on the
expected value itself, not on the average of the measured result and the
expected value -- unlike \code{evaluate_repeatability()}/
\code{evaluate_reproducibility()}, which compare two independent measured
results and so use their average.
}
\keyword{internal}

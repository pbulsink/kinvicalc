# Run the kinvicalc Shiny application.

Run the kinvicalc Shiny application.

## Usage

``` r
run_app()
```

## Value

A [`shiny::shinyApp()`](https://rdrr.io/pkg/shiny/man/shinyApp.html)
object.

## Examples

``` r
if (interactive()) {
  run_app()
}
```
